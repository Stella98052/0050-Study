# LESSONLEARNT — 0050 量化分析系統 第一階段（v1.5 / 2026-07-12）

## L1：TWSE 官方 API 會回錯月份的資料（最重要）
- **現象**：VG-1 報 2454 重複日期 21 筆、2308 重複 20 筆（≈恰一個月交易日數）
- **診斷**（diagnose_duplicates.py）：2454 的 201905.csv 內容實為 202411；
  2308 的 201709.csv 內容實為 202005。抓取為單執行緒逐月、逐股獨立資料夾，
  排除本地錯位 → **是 TWSE STOCK_DAY 對某月請求回傳了另一個月的內容**
- **防護（三層）**：①越月過濾：回應/快取列不屬於請求月→過濾+警示
  ②合併去重：仍重複→警示+保留首筆 ③診斷工具點名污染檔供刪除重抓
- **原則**：官方 API 也會回錯資料；快取層必須驗證「回應內容屬於請求範圍」，
  異常要可見（不靜默），並提供自我診斷工具

## L2：當月快取凍結（每日更新的隱形殺手）
- **現象**：202607.csv 於 7/11 首抓後被永久快取，整個七月沿用，
  7/13 之後的新交易日永遠進不來
- **修法**：yyyymm == 本月 → 一律重抓並覆寫；月份結束後自然轉固定快取
- **原則**：「進行中」的資料不可永久快取；快取策略須區分已定案/未定案期間

## L3：官方休市日表僅涵蓋當年度
- **現象**：輔助校驗誤報 165 個「疑似資料缺漏」，實為 2016~2025 的中秋/國慶/春節
- **根因**：rwd 端點僅回當年度 27 筆，卻拿去校驗十年
- **修法**：校驗僅於休市日表實際涵蓋期間 [min,max] 內進行，範圍外明示「不校驗」
- **原則**：使用參照資料前先確認其涵蓋範圍；範圍外標註未知，不可誤報

## L4：臨時休市不在年度休市表（2026-07-10 巴威颱風實例）
- **現象**：修正 L3 後仍剩 1 筆疑似缺漏 2026-07-10
- **查證**：證交所公告因巴威颱風 7/10 全日休市，23 檔除權息順延至 7/13
- **結論**：警告正確地把「無法自動分類的日子」交人工判定——這是設計邊界，
  非 bug；警語已加註臨時休市說明與本案例
- **原則**：自動校驗的殘餘警告是人工查證清單，不是全部都要修程式

## L5：版本混用重演 → 版本橫幅防呆（v1.4 起）
- **現象**：v1.3 交付後使用者重跑，輸出與 v1.2 完全相同（新版未覆蓋、
  污染檔未刪），但無從立即辨識
- **修法**：四個入口檔啟動第一行印「▶ 0050 phase1 v{VERSION}」；
  對照輸出第一眼即知版本
- **原則**（第二次驗證母專案教訓）：多檔交付必附「錯裝立即可見」機制

## L6：測試案例本身也要驗算（兩例）
- 鐵律二測試 (10,9,12) 誤標「波3非最短」——9 就是最短，規則對、測試錯
- 雙軌計數測試兩事件恰隔 5 日曆日=達獨立門檻，預期值寫錯
- **處置**：修測試不修規則（拒絕「讓測試變綠」的錯誤修法）
- **原則**：測試紅≠程式錯；先獨立驗算，判定錯在規則還是錯在測試

## L7：官方端點多候選 + 多格式解析
- OpenAPI holidaySchedule 與官網 rwd 端點格式不同（list-of-dicts 民國年 vs
  {stat,data} list-of-lists 西元）→ 依序嘗試、雙格式解析、印出實際採用來源
- **原則**：對官方資料源準備 ≥2 候選端點與寬容解析；成功來源要可追溯

## L8：伺服器異常 ≠ 本地環境問題
- 曾懷疑 OneDrive 同步造成檔案污染；診斷工具證實檔名與內容月份錯位
  的型態只可能來自回應本身 → 根因歸屬要靠證據，不靠猜測（母專案
  「臆測執行時點」教訓的再驗證）

## 驗收軌跡
v1.0 首交付(34 UT) → v1.1 auto_fetcher/降級鏈(46 UT) → v1.2 越月防護+診斷
(49 UT) → v1.3 休市表涵蓋範圍(50 UT) → v1.4 版本橫幅 → v1.5 當月快取修復
(52 UT)。實戰驗證：十檔×10年抓取、VG-1 全過、TWSE 回錯月與颱風臨時休市
兩類真實異常均被正確攔截/標記。

## L9：回測權益曲線的三層失真（v2.0→v2.3 演進，2026/7/12 單日修三次）
- **v2.0**：逐筆連乘 → 重疊持有被虛構成序列複利（holdout 1,677萬%）
- **v2.1**：等權日籃連乘 → 量級仍被時間壓縮 N 倍（+1948%）
- **v2.2**：日等效 (1+r)^(1/N) 鏈乘 → 指數律恆等於 (∏(1+r))^(1/N)，
  修了量級沒修結構——它不對應任何可執行交易路徑
- **v2.3（定案）**：不重疊交易（間隔≥N，同 VG-4 規則）原始報酬直接連乘
  = 真實可執行的單部位策略路徑，無需換算
- **原則**：重疊觀測不能序列鏈乘；權益曲線的每個數字必須對應一條
  「實際可能發生」的交易路徑，否則就是尺度修正掩蓋結構錯誤

## L10：退化案例當鎖定測試 = 自我印證陷阱（外部審查抓到，雙方都踩過）
- v2.2 的鎖定測試用「連續五日報酬全部相同」——恰是錯誤方法會蒙對的退化案例
- 外部審查者第一版驗證例（對稱交替±報酬）也巧合蒙對，自承踩同類坑
- **正解**：鎖定測試必須用「不對稱、不成組」的案例
  （[+10%,0,0,0,0,+5%]：正確 15.5% vs v2.2 錯誤 2.92%，差異立現）
- **原則**：設計鎖定測試時先問「什麼輸入會讓錯誤方法也算對？」並刻意避開

## L11：外部審查的兩種正確結局（與 c1 乘數案對照）
- c1 乘數審查（2026/7/5）：驗算後**拒絕**（審查自身算術錯誤+破壞鑑別度）
- 權益曲線審查（2026/7/12）：驗算後**採納**（四項主張逐一驗算成立，
  且審查者誠實揭露自己第一版驗證例的缺陷）
- **原則不變**：先驗算數字、再檢驗方法論一致性，才決定採用與否；
  採納與拒絕都是驗算的正常產出，預設立場（全信/全拒）都是錯的

## L12：共用規則單一事實來源
- 「獨立交易」定義同時被 VG-4 計數與權益曲線使用 → 抽出
  select_independent_dates 共用核心，兩處委派之，並以測試鎖定兩處
  逐一相同——防止日後其中一處改規則造成樣本數與曲線對不上

## L13：VG-3 偽重複——統計檢定的樣本獨立性（2026/7/13 外部審查抓到，驗算採納）
- **現象**：N 網格研究中 N=10/15/20/30 四點齊釘 p=0.0，模式本身即可疑線索
  （真實訊號漸強應有連續變化，不會整批釘 0）
- **根因**：VG-3 餵入未篩選的交易層序列（N=10 時數百筆重疊觀測），
  重疊樣本在趨勢中群聚正相關 = 偽重複（pseudo-replication），
  人為收窄虛無分布 → 壓低 p 值。受控驗算：同資訊灌 8 倍重複，p 0.266→0.054
- **更嚴重的定性**：定案4 原文早已寫明「統計獨立層輸出的樣本數才是
  VG-3 與 VG-4 唯一採用的計數依據」——實作只讓 VG-4 遵守、VG-3 漏了，
  屬違反已定案規則，非單純技術瑕疵
- **修法**：新增 independent_return_series 共用核心（同日等權合併→
  ≥N 篩選），VG-3 一律先篩再檢定；權益曲線同源委派（L12 落實）
- **連帶規則**：VG-4 未達門檻時 VG-3 結果標「未決(VG-4蓋過)」，
  與 holdout Sharpe 同一待遇——治理規則必須覆蓋每一條新流程，
  新腳本不能成為規則的法外之地
- **原則**：權益曲線修過的偽重複，同樣存在於統計檢定的獨立性假設；
  同一類錯誤會在不同位置重複出現，修一處時要問「還有哪裡用了同樣的假設」

## L14：多頭樣本中的長持有幻覺——隨機進場基準
- 淨報酬隨 N 由負轉正不能證明訊號優勢：任何長多策略在多頭樣本中
  拉長持有都會吃到市場飄移（holdout Alpha −33.66% 已是同一課）
- **修法**：N 網格加入 baseline（同池同 N 任一日進場均值）與
  edge = 訊號獨立均 − baseline；能支持假說的是 edge 隨 N 擴大
- **原則**：報酬數字必須相對於「不含訊號的同構基準」呈現，
  絕對值由市場環境決定，邊際才是訊號的貢獻

## L15：趨勢結論的預先宣告資格（v2.5，2026/7/13）
- N 網格回答的是「edge 隨 N 如何變化」——比單點更弱、更易被雜訊污染的問題
- **預先宣告規則（寫在擴大樣本之前，防「規則寫在結果之後」）**：
  ①edge 一律配對計算（逐筆扣同進場日全池均值）並附 bootstrap 95% CI，
    相鄰 N 的 CI 大幅重疊 = 雜訊連線的客觀判準
  ②至少 3 個「相鄰」N 同過 VG-4 門檻才有趨勢結論資格（程式化判定）
  ③樣本分母擴充優先序：0050 全體成分股（~50檔，不動歷史區間）→
    仍不足才延長年限（成本高且加重早期成分股存活偏誤）

## L16：同屏矛盾是最好的偵錯訊號——VG-3/VG-4 同源化（v2.6，2026/7/13）
- **現象**：同一次執行輸出「VG-3 檢定樣本 217 筆」與「VG-4 獨立訊號數 35」
  ——依定案4 兩者本該同源同數，同屏差 6 倍即定義漂移的鐵證（L12 預言成真）
- **根因①（bug）**：count_statistically_independent_signals 假設輸入按時間
  排序，但 run_phase2 餵入「依股票分塊 concat」序列——跨股邊界日期倒退，
  greedy 間隔算出負數，後續股票事件幾乎全被跳過 → VG-4≈只算第一檔
  （樣本外「獨立3」同為低估）
- **根因②（定義分歧）**：VG-3 用日期直接 greedy、VG-4 用事件合併後 greedy
  （定案4 原文為後者），兩套各自演化
- **修法**：canonical_independent_samples 正典管線（逐日聚合 any →事件→
  ≥N→代表日報酬均值），VG-3 與 VG-4 一律取自此函式，同源同數以測試鎖定；
  多股未排序輸入的回歸測試釘入
- **定義分工（刻意保留兩層，各自單一來源）**：統計層=事件法（一段連續
  訊號episode=1樣本，定案4）；路徑層=可執行再進場（日期 greedy，權益曲線
  用）——連續12日episode、N=5：統計層1樣本 vs 路徑層3筆，測試鎖定防互改
- **原則**：兩個本該相等的數字同屏出現不等，永遠優先當 bug 查，
  而不是各自解釋；輸出設計應刻意讓同源數字並列以自我暴露矛盾

## L17：N 網格的兩道統計閘與範疇標示（v2.7，2026/7/13 審查三採納一駁回）
- **B1 多重比較（採納）**：5 個 N 各自檢定，純雜訊下至少一次假顯著機率
  22.6%——單點 raw p 顯著不算數。Holm 校正入庫（免新依賴手算逐步法）；
  實跑驗算：N=15 raw 0.008 → adj 0.040 存活，其餘全不顯著
- **B2 趨勢檢定（採納）**：zigzag 形狀不在原兩分支判讀準則內 → 禁止
  肉眼連線，edge 對 N 線性迴歸「斜率>0 且 p<0.05」才可宣稱趨勢；
  實跑：斜率+0.0915%/日、p=0.0596、R²=0.745 → 未達顯著
- **A1 標籤寫反（採納）**：181 是「統計獨立層計數」（事件法篩選後），
  不是「事件法計數」（事件數是210）——輸出字串抽成可測函式並鎖定
- **A2 相等斷言（駁回，範疇標示採納）**：路徑獨立51=模型策略（LightGBM
  預測）路徑層；VG-4 獨立21=規則訊號統計層——不同策略×不同層，本就
  不該相等，強制相等斷言會錯誤炸開。真問題是輸出未標明範疇 → 修為
  明確標示【模型策略】/【規則訊號】，並為模型策略補上自身的統計層
  計數與蓋過閘
- **原則**：審查建議逐項驗算後可「部分採納」——診斷對、藥方錯的情況
  存在；範疇混淆的正解是標示與分閘，不是強迫不同對象相等

## L18：獨立性定義必須綁定對象——事件法對密集策略的語意退化（v2.8）
- **現象**：holdout 模型策略「路徑獨立51 vs 統計獨立1」，50 倍落差
- **根因**：非 bug、非兩套 N——模型策略每日重選、跨10檔，逐日 any() 聚合
  後 100% 為 True → 事件法把整段 holdout 併成 1 個事件。事件法（定案4）
  的設計對象是「稀疏 episode 型規則訊號」，套到密集策略上語意退化
- **修法（定義綁定對象）**：
  稀疏規則訊號 → 事件法（VG-3/VG-4，定案4 原意）
  密集模型策略 → 非重疊窗口法（統計樣本=權益曲線路徑層，同一呼叫同源，
  runtime 斷言鎖定），並對其跑真正的 bootstrap 檢定，非只靠計數閘
- **診斷工具**：independence_divergence_report 並排兩法計數+訊號日占比，
  自動判定適用方法（占比≥50%→path）
- **VG-2 交叉驗證（審查詢問）**：程式追溯確認主/對照兩側皆為
  規則訊號 × 同一 compute_backtest_metrics —— 對稱、未被模型策略
  分歧污染；45.71% 之比較基礎已標明於輸出
- **原則**：「同源」的正確單位是「同一對象」——不同對象各自同源、
  且輸出必須說明為何合理地不同；被蓋過≠結案，分歧要解釋到底

## L19：聚合層級誤判與 VG-6 的誕生（v2.9，2026/7/14）
- **審查推論的修正**：「訊號日占比100% ⇒ 模型退化」混淆了聚合層級——
  逐列選中率 41.5%（997/2400，明顯有篩選）的模型，跨十檔聚合後
  「任一檔/日」占比 = 1−(1−0.415)^10 ≈ 99.5%，100% 是多股池數學必然，
  不能單獨作為退化證據
- **但問題本身正確且採納**：機率分布形狀與判別力此前五道關卡皆未覆蓋
  → 新增 VG-6（逐列機率分布 std/中間帶/單邊率 + 免 sklearn 的
  Mann–Whitney AUC），退化與否用資料回答、不用推論猜；
  輸出附「聚合占比為數學必然」的防誤判註記
- **措辭分級（採納）**：趨勢迴歸 p=0.0596 屬「臨界未顯著」，與六閾值
  全平坦那種鐵證級否定必須語氣區隔——結論的份量要跟證據強度成正比
- **原則**：審查的「診斷推論」與「建議的檢查」可以分開評價——推論
  錯了不代表檢查不該做；健檢項目寧可多建，判定交給資料

## Phase 2 科學結論狀態（截至 v2.9）
- ✅ 已定案：規則訊號（第3浪+潮汐爆發）N=5 無優勢——181 獨立樣本、
  均值+0.0001%、p=0.246、CI 含零、六個 ZigZag 閾值全平坦（鐵證級）
- ✅ 已定案：成本攤薄假說不成立——Holm 後僅 N=15 臨界存活(0.040)、
  趨勢斜率 p=0.0596 臨界未顯著、edge 非單調；證據鏈完整但註明
  「趨勢部分屬臨界未顯著，非鐵證級否定」
- ✅ 已定案（2026/7/14 VG-6 實跑）：模型策略線——AUC=0.494≈擲硬幣，
  分布健康但無判別力；holdout 正績效=市場β非模型判斷（Alpha −47.9% 自洽）
  → 現有特徵集歸檔，重啟需重設計特徵+全新驗證期

## Phase 2 正式收官（2026/7/14）
三線收斂：規則訊號無優勢（鐵證級）、成本攤薄不成立（含臨界註記）、
模型無判別力（AUC 裁決）。定案報告：PHASE2_CONCLUSION.md。
誠實的否定結論 + 可複用驗證基礎設施 = 本階段交付物。

## L20：診斷工具自身也要過偽重複關——特徵稽核的獨立子樣本（v2.10）
- 審查建議的特徵稽核（point-biserial 逐特徵 vs 標籤）方向正確，但原始碼
  直接用全部重疊列（~2.4萬列、5日標籤相鄰高度相關）→ p 值人為壓低，
  會長出假顯著特徵，把「特徵無訊號」誤診成「模型沒學到」，決策樹
  第一個分岔就走錯
- 修正：稽核一律逐檔取間隔≥N 獨立子樣本；特徵數量過 Holm；
  另備逐日橫斷面 IC（每隔N日取樣+t檢定）作第二套獨立診斷
- Model v2 路線圖（審查排序採納）：①稽核分岔（特徵無訊號 vs 模型沒學到）
  →②相對化/排名化特徵（剝離大盤共同波動；IC 取代 AUC 評估弱訊號）
  →③才輪到調參。新資料源（籌碼/營收）列②之後備選
- **holdout 單次使用鐵則（成文化）**：所有特徵比較只在 walk-forward
  驗證折上進行；最終組合對 holdout 只跑一次、只跑這一次——重複測
  holdout = 在最後防線上做多重比較，比 N=15 假陽性更嚴重

## L21：提案驗證三修正（v2.11，2026/7/14——採納提案主體，修三處）
- **iloc[::N] 逐列取樣 bug**：長表（10檔×日期）上每隔N「列」≠每隔N「日」，
  會把每日橫斷面砍到 1~2 檔且未拉開日期間隔——診斷C改在 L20 獨立
  子樣本上做單一次檢定，不做逐日切片
- **median_p 非法檢定量**：逐日 p 值取中位數無已知虛無分布、無法校準
  ——改為 True/False 兩組未來淨報酬之單一次 Mann-Whitney U + Holm
- **手寫產業表先驗錯誤**：2327 國巨=被動元件（電子零組件，非電子代工）、
  3037 欣興=PCB 載板（電子零組件，非半導體）——「先驗凍結」的正確
  對象是 TWSE 官方產業分類（t187ap03_L），手寫表本身就可能引入
  研究者先驗偏誤；第四優先若啟動，一律凍結官方分類
- 提案其餘全數採納：診斷覆蓋率補齊（類別特徵）、相對化+回歸IC 同步、
  波動率第三優先、交互作用清單凍結、「找到訊號即停止搜尋」條款

## L22：第一個倖存特徵的雙假說裁決框架（v2.13，2026/7/15）
- realized_vol_20d 是全稽核唯一過 Holm 者（A: r=+0.071, p_holm=6e-5），
  但 B（連續報酬橫斷面）不顯著——A/B 落差本身攜帶診斷資訊
- 兩個必須「都」裁決的獨立假說（結局矩陣預先宣告，Holm m=3）：
  H_timing（審查）：訊號在跨時間市場狀態層 → 擇時非選股，兩層架構依據
  H_artifact（本方）：二元標籤×固定成本門檻×離散度的機械假象 → 不可交易
  P1 市場層級占比 / P2 擇時 IC / P3 均值vs離散度效應分離
- P3 是關鍵鑑別：vol 若只放大 |報酬| 而不移動均值，診斷A的顯著性
  就是分類資訊損失製造的幻影——這正是「回歸取代分類」建議的實證版
- 布林驗證閘疊加（審查補強）：to_numeric 防禦會默默放行數值非布林欄，
  值域 ⊆ {True,False,0,1} 檢查更嚴——防禦層可以疊加，不必二選一
- **原則**：第一個顯著結果出現時最危險——立即為它建立「怎樣算假的」
  的檢定，而不是為它建立敘事
- **附記（L22 補）**：P1 首版指標有偏——k 檔組均值對純雜訊機械性解釋
  1/k 變異（3檔→33%），會把個股雜訊誤報為市場層級；鎖定測試在交付前
  抓到，改 ICC 式校正 (R²−1/k)/(1−1/k)。診斷工具的指標本身也要有
  「純雜訊該得零分」的測試

## L23：VG-7 特徵真偽篩制度化 + 同款統計錯誤的第三次攔截（v2.14，2026/7/15）
- **realized_vol_20d 定案**：P3 離散度 r=0.295（極顯著）vs 均值 r=0.030
  （不顯著）、P2 擇時不顯著 → H_artifact 成立，兩層架構假說關閉。
  機制：label_up 門檻在成本（+0.5925%）而非 0，任何放大離散度者
  皆機械性提高跨門檻機率，與方向無關
- **家族性歸檔（審查採納）**：realized_vol_5d / vol_regime_ratio 與 20d
  同屬離散度量化——波動率類特徵對二元 label_up 天生帶「離散度×固定
  門檻」機械關聯，日後任何此類特徵過診斷A都必先過 VG-7 才可宣告
- **VG-7 收編（審查方向採納，程式碼替換）**：提案原始碼三處統計問題
  ①全列 pearson＝橫斷面偽重複（L13/L20 同款第三次）②日期虛擬變數
  OLS R² 與 groupby 均值法等價、同帶 1/k 偏誤（L22 附記同款）且需
  statsmodels 新依賴 ③逐日全取樣擇時＝時序偽重複——三者皆以已驗證
  版本替換。**同一類錯誤會跨審查者、跨場景反覆出現：制度化（收編為
  關卡+鎖定測試）是唯一的長期解**
- **人工複核格（審查貢獻採納）**：驗證時發現本方矩陣「僅擇時顯著」
  一格誤入「皆不顯著」分支——這次沒踩到純屬運氣。八格窮舉補全，
  未落乾淨格一律標「需人工複核」，不自動套布林
- **自動觸發**：稽核判定顯著即自動跑 VG-7，兩層結果一次輸出，
  不依賴人工記得多跑一步

## L24：同屏矛盾第二型 + 分組稽核規則（v2.15，2026/7/15）
- **同屏矛盾（自查）**：v2.14 稽核輸出上一行 VG-7 判「歸檔」、下一行舊
  分支仍印「新特徵含訊號→前進折內評估」——判定訊息未依 VG-7 收斂。
  修法：final_search_verdict() 以 VG-7 裁決為唯一準據（存活/歸檔分流），
  測試鎖定「歸檔者不得再印前進」。與 L16 同原則：本該一致的輸出
  同屏打架，優先當 bug 查
- **Holm 家族定義成文（審查提醒）**：VG-7 內 m=3 為「每特徵各自一個
  確認家族」；選擇階段多重性由診斷 A/C 跨特徵 Holm 控制。≥2 特徵
  同時觸發仍各自 m=3，但任何子檢定 p_holm 落 (0.01, 0.10) 邊緣帶
  一律標 ⚠ 人工複核——定義寫在撞上之前，不寫在撞上之後
- **分產業稽核預先宣告規則**：①分組凍結 TWSE 官方 t187ap03_L
  （雙鍵解析+CSV fallback+皆不可用明確 raise，禁手寫表）②組內獨立
  樣本 <200 → 標「僅供參考」不納入宣告、不觸發 VG-7 ③診斷B需組內
  ≥5 檔 ④Holm 每組一族 ⑤合格組顯著特徵照常過 VG-7（分組樣本更小、
  偶然顯著更高，真偽篩更必要）

## L25：外部程式碼庫審查的逐項裁決（v2.16，2026/7/15——一採納四駁回）
- **✅採納：原子寫入快取**（temp+os.replace）——半寫髒檔與 TWSE 錯月
  快取同族風險，成本低收益實，鎖定測試含「寫入失敗不留殘檔」
- **❌駁回 Option B（canonical core）**：審查不知該核心 v2.6 已存在
  （signal_events.canonical_independent_samples + v2.7 runtime 斷言 +
  鎖定測試）。其實作版本兩處分歧：①「日曆日連續」切事件——週五+週一
  （連續交易日）被切成 2 事件，違反定案4 交易日語意（驗算確認）
  ②附帶迴歸測試空洞——pool==observed 使 naive_p 恆=1.0，斷言
  ind_p≥naive_p 不可能失敗（L10 同款：測試無法區分對錯方法）
- **❌駁回「統計/路徑層強制單一核心」**：L16/L18 已確立雙層設計
  綁定對象（稀疏規則訊號=事件法／密集策略=非重疊窗口），強制單核
  會重新引入密集策略退化問題——審查把刻意設計誤判為隱藏耦合
- **❌駁回 Option A 並行抓取**：規格原文要求「每次請求間 1–2 秒延遲、
  符合 TWSE 使用條款合理頻率」——6 workers 併發直接違反規格；
  快不是目標，合規與資料正確才是
- **❌駁回 SQLite index/pydantic/pandera/CI 等**：與「最小依賴」原則
  衝突（phase1 僅 requests）；CI/mypy 記為選配，非本階段範圍
- **原則**：外部審查若不知道程式碼現況，其「補缺」建議可能是「倒退」；
  裁決基準永遠是逐項驗算 + 與既有定案的一致性，不是建議的完整度或
  專業外觀。空洞測試（無法失敗的測試）比沒有測試更危險——它提供
  虛假的安全感

## 第三階段落成（v3.0，2026/7/15）
- 面板/每日更新/前瞻紀錄三支程式 + src/dashboard 六純函式模組，全部委派
  phase1/phase2 既有函式（單一事實來源），102 測試全過
- 誠實性延伸至 UI：無報告不顯示全綠、VG-6 未通過預測卡強制警語、
  特徵欄不符明確報錯、13MV 否決獨立標示——治理準則不因進入展示層放鬆
- 前瞻驗證管線落地：predictions.csv 去重+獨立樣本計數，Model v2 最終閘
  =累積≥30獨立樣本後一次性裁決（預先宣告，取代已被多次評估的 holdout）
- 規格三階段全數交付；後續為選項：訊號重構（需全新驗證期）／50檔擴充／
  第四優先分產業（v2.15 已就緒待跑）

## L26：面板預測全股失敗——對齊檢查比對錯層級（v3.1 修，2026/7/15）
- **現象**：面板預測卡每一檔股票都報「特徵欄不一致，缺少 wave_1…wave_C,
  wave_unknown；拒絕套用模型」——全股一致失敗即「與個股無關的結構問題」
- **根因**：模型 wrapper.predict_proba 設計為「吃原始特徵 df、內部 _encode
  自動 one-hot 波浪標籤」（固定類別集 _WAVE_CATS）。但 model_service 的
  assert_feature_alignment 拿模型記錄的『展開後』特徵名（wave_1…）去比對
  面板送的『原始』欄（wave_label_realtime），在 wrapper 編碼之前就檢查 →
  必然報缺欄
- **修法**：對齊檢查改驗證原始 FEATURE_COLS 齊備（wrapper 會自行展開），
  非展開後欄名；真缺原始欄仍明確 raise（不靜默）。端到端鎖定測試：
  原始特徵矩陣→出機率 + 缺原始欄→報錯
- **原則**：跨階段介面（訓練序列化 vs 推論載入）的「特徵契約」必須明確
  定義比對層級（原始欄 or 編碼後欄）；wrapper 內建編碼時，對齊檢查要在
  編碼前的層級進行。全股一致的失敗永遠指向結構/契約問題，非資料問題

## L27：面板兩類資訊分區——全模型 vs 單股（v3.2，2026/7/15）
- **使用者觀察**：VG 卡「每檔股票一致」引發疑慮
- **判定：非 bug，是版面語意誤導**。VG-1~6 為全模型層級驗證（十檔同一
  模型訓練→每檔相同是正確的），但擺在單股預測卡下方使讀者誤以為是
  單股檢查。同一區塊還混入了真正逐股變動的 realtime 波浪/13MV，加劇混淆
- **修法（改標示非改數字）**：①預測卡標「本股即時值（隨股票變動）」
  ②VG 卡標題改「全模型層級，非單股」+說明「每檔相同是設計非錯誤」
  ③加科學結論摘要卡，讓 VG-3❌/VG-6❌ 有脈絡（誠實否定結論、非故障）
- **原則**：當固定值與變動值同屏並列，必須各自標明範圍（全體 or 單股）；
  「每檔都一樣」對全模型指標是正確、對單股指標才是 bug——版面要讓
  使用者一眼分辨兩者，否則正確的一致會被誤讀為可疑

## 第三階段雲端部署（v3.3+，2026/7/15）
- 平台選 Streamlit Community Cloud（Python 後端需伺服器，靜態託管不適用）
- 三個部署現實與對策：①雲端無快取→首載逾時：預抓+commit data/raw
  ②模型包不在 repo→預測空白：git add -f data/models ③排程更新：
  GitHub Actions 每交易日 22:00(台北) 跑 daily_update 並 commit predictions
- 誠實性延伸至公開情境：風險聲明/VG❌/自選股不適用說明全保留；
  面板無任何金鑰（只讀 TWSE 公開資料），可安全公開
- 部署 pack：.streamlit/config.toml、packages.txt、.github/workflows/
  {daily_update,tests}.yml、bootstrap_cloud.py、DEPLOY.md、.gitignore

## 流程自動化落地（2026/7/15）
- repo 檢查發現：自動化檔案（.github/workflows 等）尚未上傳、__pycache__
  誤入版控、模型包無法外部確認 → 提供 setup_github.sh 一鍵修正
- 兩條流水線：①daily_update.yml 每交易日 22:00(台北) 累積前瞻紀錄並
  commit（無模型包則自建）②tests.yml 每 push 跑 105 測試
- 自動化邊界（治理）：只累積資料/前瞻，不自動重訓擴充池——訓練池變更
  須經明確 holdings 更新與 VG 驗證，不被排程偷偷觸發

## L28：CI 環境版本落差——精確釘版的雙面刃（2026/7/15）
- 現象：本機 105 測試全綠，但 GitHub Actions CI 兩次皆紅叉
- 最可能根因：requirements.txt 精確釘死本機 Python 3.14 的超新版本
  （pandas 3.0.2/scipy 1.18.0/pytest 9.1.1），CI Python 3.12 的 pip
  解析不到 → 安裝步驟即失敗（尚未跑到測試）
- 修法：①requirements 改 >= 下限（保留本機實測版於註解）②CI Python
  升 3.13 接近本機。原則：精確釘版利於「重現」但害於「跨環境移植」；
  函式庫專案宜下限、應用程式宜 lock file，本專案採下限+註解記錄實測版
- 治理紀律：CI 紅叉不可無視或直接部署——先看 log 定位（安裝 vs 測試
  失敗，修法不同），改動須被 log 證據支持，不僅憑推理

## L29：CI 缺相依——scikit-learn（2026/7/15，log 確認的真根因）
- 現象：本機 105 全綠、CI 兩測試紅叉（test_run_walk_forward_end_to_end、
  test_model_service_accepts_raw_feature_matrix）
- **真根因（log 直指）**：LightGBMError「scikit-learn is required for
  lightgbm.sklearn」——train.py 用 lgb.LGBMClassifier（sklearn 介面），
  必需 scikit-learn，但 requirements.txt 漏列。本機早有 sklearn 故通過，
  CI 乾淨環境沒有 → 訓練模型的兩測試炸
- **教訓（方法論層）**：我曾先推測 libgomp（含 C 擴充的合理猜測），
  但那是錯的方向——**沒有 log 就修等於賭**。堅持取得 log 才定位到
  真正缺的是 scikit-learn。libgomp 補了無害（LightGBM 亦需），但非本次主因
- 修法：requirements.txt 加 `scikit-learn>=1.3`（本機實測 1.8.0）
- **驗證方法升級**：不再只靠本機 pytest（本機有既存套件會遮蔽缺漏），
  改用全新 venv 裝 requirements 重跑失敗測試 → 2 passed，才算對 CI 有效
- 原則：requirements 完整性只能用「乾淨環境」驗證；本機綠 ≠ CI 綠，
  差異往往是本機既有、requirements 卻漏列的隱性相依

## L30：環境自檢測試——把相依缺漏往前攔一層（審查採納，2026/7/15）
- 審查建議加「核心套件可匯入」自檢，讓相依缺漏最先失敗、訊息直接
  （勝於從 LightGBMError 反推）——採納
- 兩處強化（避免建議自身引入新問題）：
  ①從 requirements.txt 動態解析套件清單，測試不另存硬編碼清單——
    否則就重蹈「兩處清單各自維護、下次又漏」（正是審查建議2 想防的坑）
  ②用 importlib.util.find_spec（只查存在、不執行套件初始化）取代
    __import__（有副作用/較慢）
- 驗證方法：乾淨 venv（不裝 sklearn）跑自檢 → 明確點名 scikit-learn，
  證實預防效果為真而非自我印證
- 專項守衛：test_lightgbm_sklearn_interface_available 直接驗 LGBMClassifier
  介面可用，本次事故類型日後在專屬測試明確暴露
- 原則：預防性測試的清單來源必須與部署相依「同源」（單一事實來源），
  否則預防機制本身會成為下一個不同步的破口

## L31：面板自動讀取每日前瞻紀錄 + 排程時間依據（v3.4，2026/7/15）
- **CI 綠**：補 scikit-learn 後 GitHub Actions 單元測試 108→109 全過，
  每日前瞻更新 workflow 亦綠；兩條流水線健康
- **排程 22:00 的依據**：台股 13:25 收盤，但盤後有集合競價/鉅額/盤後定價
  (14:00-14:30)等處理，STOCK_DAY/T86 等 API 通常傍晚才穩定齊備且
  TWSE 未保證時間、偶有延遲。22:00 為保守安全邊際（此系統累積前瞻樣本、
  非即時交易，晚幾小時無損、資料正確優先）。想更即時可改 18:30 並靠
  「資料非今日即警示」防護把關
- **面板自動讀取**：新增「每日前瞻紀錄」區塊——latest_prediction_per_stock
  各股最新預測總覽表 + load_predictions_view 單股歷史趨勢圖，自動讀
  Actions 每日 commit 的 predictions.csv；Streamlit Cloud 隨 repo push
  自動更新即讀到最新。VG-6 現況警語保留（僅演示）

## L32：不臆斷已知事實、不生成不必要的贅述（2026/7/15，使用者明確要求）
- 使用者已在兩天前確認「本機 Streamlit 面板正常運作」為既定事實，
  助理卻在後續回覆重複「這代表你本機面板跑起來了」——這是把使用者
  早已知道的事當新發現宣告，屬冗餘臆測，浪費篇幅且顯得未在聽
- **規則**：①已確認為事實者不再重述為「好消息/這代表…」②回覆聚焦
  使用者當下問的問題，不加開場鋪陳與情緒性評語 ③診斷問題時直接給
  定位指令與修法，不先鋪陳一段「這其實是…」的解說
- **packages.txt 教訓（技術面）**：Streamlit Cloud 的 packages.txt 走 apt，
  **不支援 # 註解**（與 requirements.txt 不同），任何註解行/中文會被
  當套件名安裝而失敗。此檔只能一行一個純套件名

## 文件整合（v3.5，2026/7/15）
- README、USER_GUIDE_PHASE3、DEPLOY、AUTOMATION 四份合為單一 README.md
  （含快速開始/部署/自動化/使用/方法論/結構/除錯全節），移除後三份
- 除錯節涵蓋所有踩過的部署坑：packages.txt 註解、scikit-learn 缺漏、
  git 目錄錯誤、本機 Deploy 按鈕、Windows 反斜線、Cloud 快取 Reboot

## L33：Streamlit 部署成功 + use_container_width 淘汰（v3.6，2026/7/15）
- **部署成功**：log「🔄 Updated app!」「Uvicorn server started」代表面板已
  上線；開頭 healthz「connection reset」為啟動瞬間健康檢查抖動，非錯誤
- **73 個「failure」實為同一則 deprecation warning 重複**：use_container_width
  將於 2025-12-31 後移除。非 error、app 照常運作，但需修否則年底失效
- **修法（分元件處理，非全域替換）**：
  - st.plotly_chart → width='stretch'（穩定）
  - st.dataframe → **移除 width 參數**（預設即填滿容器）：因搜到未確認
    的 bug #12846「dataframe width='stretch' 觸發 TypeError」，避開風險
- **原則**：淘汰警告要修（年底會真的壞），但替換前查證新 API 在各元件的
  穩定性——同一個新參數在不同元件成熟度不同，不宜無差別全域取代

## L34：一律交付完整 pack 覆蓋，禁止單檔交付（2026/7/15，使用者重複要求後仍再犯）
- **事故**：只推 app_streamlit.py（v3.4 新增「每日前瞻紀錄」需
  load_predictions_view）而未同步 predictions_log.py → 雲端
  ImportError: cannot import name 'load_predictions_view'，部署失敗
- **這是重複犯錯**：使用者先前已明確要求「用完整 pack 一次覆蓋所有檔」，
  助理為求省事仍多次給單檔修正（packages.txt 單檔、app_streamlit.py 單檔），
  造成跨檔版本不同步。單檔交付把「哪些檔互相依賴」的心智負擔丟給使用者，
  一旦漏推任一依賴檔就在雲端炸——本機測試還測不出來（本機檔案是全的）
- **鐵則（最高優先，凌駕省 token/省事）**：
  ①任何修改一律輸出完整 pack（Phase1_complete_vX.Y.zip，內層 phase1、
    含全部檔案），使用者整包覆蓋，不再挑單檔
  ②即使只改一行，也交付完整 pack——因為助理無法保證使用者手動挑檔時
    不漏掉依賴檔
  ③不再說「只改一個檔就好」「覆蓋這一個檔即可」這類話
- **原則**：交付完整性 > 交付精簡。省下的幾 MB 遠不值一次雲端部署失敗
  與使用者的來回除錯時間；且「單檔夠不夠」的判斷風險應由助理內部消化
  （出完整 pack），不該轉嫁成使用者的漏推風險

## L35：溝通精簡——不重複、不贅述（2026/7/15，使用者要求；本條曾被
重複寫入三次，2026/7/16 去重合併——正是本條要防的錯，引以為戒）
- 探討過程不重複生成已說過的內容；只提供解決方案與有幫助的字句
- 刪除情緒性/自責式贅語（「害你部署炸」「這是我的問題」等）——
  認錯一句帶過，重點放修法
- 回覆結構：根因→修法→指令；不加開場與收尾鋪陳
- 與 L32 一脈相承：不臆斷已知事實、不生成無資訊量字句

## L36：自選股 403 修復與部署運維教訓（2026/7/16，本機已驗證）
- **TWSE 403**：舊端點 STOCK_DAY 拒絕無瀏覽器標頭請求。前十大靠
  data/raw 快取未暴露；即時抓自選股才觸發。修法：twse_daily 帶
  _TWSE_HEADERS（UA/Accept/Referer），holdings MOPS 同補。
  本機實測：2337 成功抓到近三日 K 線（2026/7/16）
- **自選股 UX（v3.9）**：輸入代號自動加入+自動切換+清空輸入框——
  先前只加入清單不切換，使用者感受為「無動作」
- **GitHub Actions 排程延遲**：排 20:00 實跑 23:24（延遲 3.4 小時）。
  cron 不保證準時、整點最重。對策：錯開整點（18:10/19:10）+
  雙時段備援（predictions 同日去重，第二次自動略過）
- **OneDrive 內 repo 的 git 災難**：.git 被同步程式鎖住 → rebase 卡死、
  rebase-merge 刪不掉、ref lock 失敗。短解：暫停同步後 rm -rf
  .git/rebase-merge 改走 merge；長解：repo 搬出 OneDrive

## LESSONLEARNT 更新頻率（規範）
- 觸發時機二擇一先到者：①下一版程式更新時 ②每週一次
- 不在同一版內反覆追加瑣碎條目；累積至版更或週更時整理寫入

## L37：雙審查裁決 + 資料下載功能（v3.10，2026/7/16）
- **MD 措辭審查：大部分採納**（知識論精確化，成本低價值高）：
  ①「零猜想」精確化為「無隱藏假設，人工設定須明示可追溯」②鐵律
  →理論衍生規則化判別條件 ③Fibonacci=標註輔助 ④背離=操作性代理
  ⑤成本一階近似明示 ⑥個股/ETF 成本層級區分 ⑦README「無可證明優勢」
  →「目前條件下未觀察到可重現優勢」⑧外部效度限制段 ⑨閾值三類標記
  約定（theory-derived/engineering-default/empirically-selected）
  ⑩ZigZag 條件性聲明。全部落於 README/FORMULAS
- **Repo 審查：六項有四項已實作**（審查自述未取得程式碼、僅依文件
  推測）：資料源隔離+fallback 日誌✓（SmartTWFetcher）、抓取驗證閘✓
  （_keep_requested_month/去重/失敗記錄+測試）、canonical 樣本建構✓
  （v2.6）、規則/模型輸出分離✓（L16/L18）。**教訓：外部審查未見碼時，
  「建議清單」實為「文件推測清單」，逐項對照現況可省大量重工**
- 新採納二項：閾值來源標記（入 FORMULAS 約定）、成本近似明示（文件）
- **功能（使用者需求）**：歷史資料收集本已自動（十年逐月快取），
  補面板可視性：涵蓋範圍說明 + 下載K線CSV + 下載特徵CSV
  （utf-8-sig BOM、特徵匯出剔除未來報酬欄防誤用）

## L38：雲端/本機資料語意 + 自選清單持久化（v3.11，2026/7/16）
- **使用者疑問「本機找不到自選股資料但網頁有圖」**：面板跑在 Streamlit
  Cloud，自選股由雲端機器即時抓 TWSE 並存雲端快取（重部署即清空、
  自動重抓）；不經使用者電腦、不 commit 回 repo（Actions 只 commit
  前十大）。要取得資料＝用面板下載按鈕。此為架構語意，非 bug
- **自選清單持久化**：公開面板無帳號，伺服器端儲存會讓所有訪客共用
  （錯誤設計）；正解＝清單序列化進網址參數（?watch=6669,2337），
  使用者書籤即存檔。解析端防禦：僅收 4–6 位數字、去重保序、上限 30
- **v3.10 部署殘留**：複製中斷（.git Permission denied 輪）漏了 config
  → 標題 v3.9.1 但下載按鈕（v3.10 功能）已出現——「版本號與功能
  不一致」是部分覆蓋的特徵訊號，完整 pack 覆蓋即癒

## L39：績效衝刺 P1+P2 首發（v3.14，2026/7/17）
- **P1 潮汐快照統一**：13MV 三結論編碼為狀態機（🟢真波段/🟡僅短線/
  🔴波段結束/⚪觀望，否決旗標凌駕同步上揚），前十大與自選股共用
  同一渲染函式（單一事實來源）；身分標籤固定「方法論檢核值，非經
  統計驗證之預測訊號」——顯示層也不得誇大
- **P2 首項 BWIBBU 估值**：官方個股日本益比/殖利率/股價淨值比。
  防前視性質：官方以當日收盤÷已公告最新EPS計算，天然 point-in-time。
  治理比照 twse_daily（逐月快取/重試/標頭/越月防護/原子寫入）；
  缺日=NaN 不前向填充（不以舊值冒充當日官方值）
- **容器封 TWSE → 端點不可本地驗證**：解析器以官方文件格式建 fixture
  鎖定測試；實抓煙霧測試留使用者本機一條指令。月營收/IMTM 同理
  待端點驗證後下一包
- 稽核 --fundamentals 旗標：估值三欄進診斷 A/B，VG-7 自動觸發照舊

## L40：同機構不同端點的格式不可假設一致（v3.14.2，2026/7/17）
- BWIBBU 實抓 200/stat=OK 但解析全空：日期格式為「115年06月01日」
  （中文），與 STOCK_DAY 的「115/06/01」（斜線）不同——沿用解析器
  直接炸，v3.14 又靜默吞掉+快取空結果，三層錯誤疊加
- 修法鏈：①雙格式日期解析 ②失敗必印原因（v3.14.1）③空結果不快取
  （v3.14.1）④fixture 改用實抓樣本節錄，不用文件推測格式
- **原則**：同一機構的不同 API 端點，欄位格式/型別（此例：日期格式、
  股利年度為 int 非 str）不可假設一致；解析器 fixture 一律以實抓樣本
  為準，文件格式僅供初版、實抓後必須回填修正

## L41：同一端點的格式也會跨年代改版（v3.14.3，2026/7/17）
- BWIBBU 舊年代（~2017 前）為 4 欄「日期,本益比,殖利率,股價淨值比」，
  新版 6 欄且順序不同——固定位置索引在舊列 IndexError 或語意錯位
  （位置1舊=本益比、新=殖利率，錯位比報錯更危險）
- 修法：以官方 fields 欄名動態對映（殖利率/本益比/淨值比關鍵字），
  兩代 fixture 各一鎖定測試；缺欄→NaN 不猜
- L40 的推廣：**格式假設的失效面不只跨端點，也跨年代**——長歷史抓取
  的解析器必須欄名驅動，不可位置驅動

## L42：py_compile 驗不出執行順序（v3.14.4，2026/7/17）
- --fundamentals 首跑 UnboundLocalError：FUND_COLS 定義在診斷B段、
  診斷A段先引用——語法合法（py_compile 過）但執行順序錯；容器封
  TWSE 使該路徑從未實跑，順序 bug 潛伏到使用者首跑才爆
- 修法：常數提升模組層級（與 V2_FEATURE_COLS 並列）+ AST 驗證
  （模組層級存在、main 內無區域遮蔽）
- 原則：新增旗標路徑若在交付環境不可實跑，至少以 AST/乾跑驗證
  「引用點皆在定義點之後」；py_compile 只保證語法不保證順序

## L43：P2 後兩發實作（v3.15，2026/7/19）
- **月營收 YoY**：MOPS 官方月報檔（全市場單月一檔，十年僅 ~134 請求）；
  **發布日對齊寫死**：M 月營收自次月 10 日起可用（法定時滯保守值），
  鎖定測試「7/9 只能用5月、7/10 起才能用6月」；基期缺→NaN 不猜；
  解析欄名驅動（公司代號/當月營收，「去年當月營收」不誤抓）
- **IMTM 首版=池內同業動能**：官方分類分組、同業均值**排除自身**
  （鎖定測試）、單獨產業→NaN；零新端點風險。誠實限制：同業僅及
  池內成員，全產業版待母體化後接產業指數
- 常數模組層級 + AST 順序驗證（L42 制度化執行）；requirements 補 lxml
- 待使用者本機驗證：MOPS 端點/編碼（L40-L42 流程備妥：失敗必印、
  空不快取、欄名驅動）

## L44：MOPS 月報解析零列的雙因＋官方CSV正解（v3.15.1，2026/7/19）
- 症狀：HTML 抓到（37–43萬字元）但解析零列。診斷（印頁首原文）揭示：
  ①表頭被 <br> 切開，pd.read_html 攤平後帶空白（「公司 代號」）——
  關鍵字比對失手 ②頁面本身內建「另存CSV」官方下載表單
  （POST /server-java/FileDownLoad, t21sc03_{y}_{m}.csv, big5）
- 修法：CSV 為主路徑（結構化、cp950、實際表頭「營業收入-當月營收」
  欄名驅動）、HTML 備援（欄名正規化：去除所有空白後比對）
- 原則：①欄名比對前一律正規化（去空白），HTML 攤平的空白是常見殺手
  ②頁面若提供官方結構化下載（CSV），永遠優先於解析呈現用 HTML
- 202607 len=869 = 當月檔未產生，屬正常（月報次月上旬才有）

## L45：研究收官封版（v3.16，2026/7/19，使用者裁定）
- **定案**：四維度 39 特徵系統性搜索無可重現訊號，Model v2 不啟動。
  rev_yoy（文獻常見因子）IC=0.0004 乾淨陣亡——比「偶爾有效」更有價值：
  不用不穩定弱訊號推動模型上線
- **反轉族的教訓落實**：上輪 B 診斷過 Holm 的 ret_5d 族，本輪僅取樣
  相位位移即 p_holm 1.0——預宣告規則擋住了「宣告後被打臉」，
  相位敏感=雜訊的實證展示
- **兩層定位封存**：上層誠實否定研究（證據鏈可復現）＋下層潮汐檢核
  面板（每日更新、模型欄位標無判別力）
- **自選股儲存工具**（收官附件）：同軌（同抓取/特徵引擎）、隔離
  （data/custom/）、防誤用（無未來報酬欄）、凍結清單（防選入偏誤）
- 待辦僅餘：①push 封版 ②Model v1 前瞻裁決 ~7/30 ③--by-industry
  完備性補查（非找訊號）

## L46：面板內自選股儲存（v3.17，2026/7/20，使用者要求單一環境操作）
- 需求澄清：儲存功能須在面板內完成，不分開至 CLI 環境
- 物理限制→設計：瀏覽器禁止伺服器直接寫檔至使用者電腦，面板內儲存
  的正確形態＝伺服器端打包（build_watchlist_zip，與 CLI 完全同引擎）
  →一鍵下載 ZIP
- 內容治理沿用：每檔 K線+特徵（無未來報酬欄）+選配估值；失敗股
  誠實列摘要不進包；與模型池隔離
- CLI（fetch_custom.py）保留為批次/自動化用途，兩者同引擎不同入口

## L47：pd.read_html 的三件套相依（v3.17.1，2026/7/20）
- CI 紅：ImportError beautifulsoup4——pd.read_html 的 html5lib flavor
  需 bs4 搭配；requirements 有 lxml/html5lib 卻漏 bs4（本機早裝故未爆，
  與 scikit-learn 事件同款隱性相依）
- 修法：requirements 補 beautifulsoup4>=4.12；乾淨 venv 驗證兩個
  解析測試通過後才出包
- 原則補充：pd.read_html 完整相依=lxml+html5lib+beautifulsoup4 三件套，
  缺一在不同 fallback 路徑各有炸點

## L48：watchdog 監督 agent + 模型/潮汐並列判讀（v3.18，2026/7/20）
- **判讀原則入檔**：模型方向（P≈48%）已證無判別力=雜訊，忽略；
  潮汐快照為方法論檢核值（量能乖離=5MV/20MV−1，>20%=攻擊量爆發；
  5MV↑+13MV↑=🟢真波段）。兩者並列非矛盾——系統誠實不假裝模型有意見
- **watchdog（21:10 台北）**：檢查 predictions 今日紀錄→缺則自動補觸發
  daily_update + 開 Issue（email 通知）；休市日誤報屬預期、Issue 註明
  可忽略。watchdog 本身亦受排程延遲，但當晚任何時間補查皆有效
- 面板配套：前瞻紀錄 >4 天未更新顯示警示（連假期間屬正常）
- 誠實限制：GitHub 免費排程無 SLA，watchdog 是「降低漏跑機率+可見化」
  非「保證準時」；預測去重使補跑安全

## L49：解讀結論功能列（v3.19，2026/7/20）
- 規則驅動解讀引擎（interpret.py）：每句由方法論條文機械對應
  （POINT 003/005、13MV 三結論、VG-6 定案），零自由生成
- 三段結構：方法論判讀（含水位×方向互補：爆量旗標+非上攻狀態=
  餘溫警戒非進場）／模型數字定位（固定標註無判別力應忽略）／
  行動語意（四狀態各對應條文）
- 前十大與自選股同一渲染路徑；自選股模型段改「不適用」語句
- 身分聲明固定尾註：方法論檢核彙整，非投資建議非驗證訊號

## L50：自選股持久化雙軌 + 全市場名稱（v3.20，2026/7/20）
- **持久化真相**：雲端檔案系統每次重新部署即清空（每日 Actions push
  就觸發）——伺服器端存檔必然隔日蒸發；瀏覽器又禁止網頁寫檔至本機。
  跨部署唯一永久儲存＝repo 內 custom_watchlist.csv（面板啟動自動載入，
  與 CLI 凍結清單同一檔）；網址書籤為本瀏覽器輔助軌
- **自選股名稱**：官方 t187ap03_L 全市場代號→簡稱（同 industry_map
  端點；簡稱優先/名稱備援/欄名驅動），快取一天、失敗回空不擋面板；
  holdings 同源名稱覆蓋優先。自選股顯示「6669 緯穎（自選）」
- 治理不變：名稱禁手寫表、皆officialsource可追溯

## L51：前瞻準確度檢驗（v3.21，2026/7/20）
- 面板新增「📐 預測準確度檢驗」＝7/30 Model v1 裁決同一套工具：
  只評已到期樣本（需 ≥5 根後續官方K）、命中=模型方向與實際 5 日
  淨報酬正負一致、淨報酬=毛−總成本率（與訓練管線同定義）、
  獨立計數沿用 prospective_progress 同規則
- 預宣告紀律內建於工具：獨立 <30 顯示「不得下結論」；≥30 依二項
  檢定 vs 50% 出裁決文字（顯著＝罕見結果須複核；不顯著＝前瞻確認
  無判別力、Model v1 歸檔）——裁決標準先於資料寫死，防事後遷就
- 凍結預測永不重算；評估只讀存檔+官方K
